[buildout]
extends =
    buildout.cfg
    https://raw.githubusercontent.com/IMIO/buildout.jenkins/master/qa.cfg

allow-picked-versions = true
show-picked-versions = true

source-directory = ${buildout:directory}


[code-analysis]
directory = cpskin
jenkins = True
