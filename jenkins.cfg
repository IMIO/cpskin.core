[buildout]
extends =
    buildout.cfg
    https://raw.githubusercontent.com/IMIO/buildout.jenkins/master/jenkins.cfg

allow-picked-versions = true
show-picked-versions = true

source-directory = ${buildout:directory}/cpskin
